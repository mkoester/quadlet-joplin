# Local overrides — values here take precedence over joplin.env.

# Required: public URL of your Joplin Server (used by clients to connect)
#APP_BASE_URL=https://joplin.example.com

# Required: PostgreSQL password — must match between the DB and app containers
#POSTGRES_PASSWORD=changeme
